{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://stinkycobbler.dev/schemas/evidence-ref.schema.json",
  "title": "Evidence Reference",
  "type": "object",
  "required": ["id", "kind", "source", "locator", "contentHash", "observedAt", "sensitivity", "toolCallId"],
  "properties": {
    "id": {"type": "string", "pattern": "^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$"},
    "kind": {"enum": ["file", "git", "tool", "document"]},
    "source": {"type": "string", "minLength": 1},
    "locator": {"type": "string", "minLength": 1},
    "contentHash": {"type": "string", "minLength": 1},
    "observedAt": {"type": "string", "format": "date-time"},
    "sensitivity": {"enum": ["public", "internal", "confidential", "restricted"]},
    "toolCallId": {"type": "string", "pattern": "^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$"}
  },
  "additionalProperties": false
}
